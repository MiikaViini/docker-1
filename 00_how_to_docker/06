apt-get update && apt-get upgrade -y
apt-get install build-essential vim git -y

# git clone https://github.com/MiikaViini/dockertest.git test
# create token in GitHub and use the token as password
# Password access has been stopped in 2021
# 